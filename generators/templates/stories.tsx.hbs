import { Story, Meta } from '@storybook/react'
import {{pascalCase name}} from '.'

export default {
  title: 'Library/{{pascalCase name}}',
  component: {{pascalCase name}}
} as Meta

const Template: Story = (args) => <{{pascalCase name}} {...args} />

export const Default = Template.bind({})